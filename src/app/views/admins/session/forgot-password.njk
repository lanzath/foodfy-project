{% extends 'admins/session/layout.njk'%}

{% block content %}
    <div class="sessionTitle">
        <h1>Nova Senha</h1>
    </div>

    <form id="forgotForm" action="/forgot-password" method="POST">
        <div class="fields">
            <div class="item">
                <p>Email</p>
                <input type="email" name="email" placeholder="example@gmail.com" value="{{user.email}}">
            </div>

            {# <div class="item">
                <p>Nova Senha</p>
                <input type="password" name="password">
            </div>

            <div class="item">
                <p>Repetir Senha</p>
                <input type="password" name="passwordRepeat">
            </div> #}
        </div>
    </form>

    <div class="buttons">
        <button id="forgotSubmitButton" type="submit" form="forgotForm">Nova Senha</button>
    </div>
{% endblock content %}
